# TODO-API

## Instruções

Executar o comando para compilar os projetos

`dotnet build`

Executar o comando para executar o projeto de API

`dotnet run --project "RR.ToDo.API" --launch-profile https`

Após que acessar o swagger para as operações

`https://localhost:7275/swagger/index.html`